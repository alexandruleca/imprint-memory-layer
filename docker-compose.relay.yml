version: "3.8"

services:
  knowledge-relay:
    image: ghcr.io/alexandruleca/knowledge-relay:latest
    build:
      context: .
      dockerfile: Dockerfile
    command: ["relay", "--port", "8430"]
    deploy:
      replicas: 1
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.knowledge-relay.rule=Host(`sync.yourdomain.com`)"
        - "traefik.http.routers.knowledge-relay.entrypoints=websecure"
        - "traefik.http.routers.knowledge-relay.tls.certresolver=letsencrypt"
        - "traefik.http.services.knowledge-relay.loadbalancer.server.port=8430"
    networks:
      - traefik-public

networks:
  traefik-public:
    external: true
